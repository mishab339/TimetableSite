<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Daily Timetable</title>
  <style>
    /* General Reset */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: Arial, sans-serif;
  background: #f9f9f9; /* Light gray background */
  color: #333; /* Text color */
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}

/* Timetable Container */
.timetable-container {
  position: relative;
  width: 90%;
  max-width: 700px;
  background: white;
  padding: 20px;
  border-radius: 10px;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Subtle shadow */
  text-align: center;
}

/* Show All Timetables Button */
.all-timetable-btn {
  position: absolute;
  top: 15px;
  right: 15px;
  background-color: #4CAF50; /* Green button */
  color: white;
  border: none;
  padding: 10px 15px;
  font-size: 14px;
  font-weight: bold;
  border-radius: 5px;
  cursor: pointer;
  transition: all 0.3s ease;
}
.all-timetable-btn a{
  text-decoration: none;
  color: #ddd;
}
.all-timetable-btn:hover {
  background-color: #45a049; /* Darker green */
  transform: scale(1.05); /* Slight zoom effect */
}

/* Timetable Box */
.timetable-box h2 {
  margin-bottom: 15px;
  font-size: 24px;
  color: #333;
}

table {
  width: 100%;
  border-collapse: collapse;
}

th, td {
  padding: 12px;
  text-align: center;
  border: 1px solid #ddd; /* Light border */
}

th {
  background-color: #4CAF50; /* Green header */
  color: white;
  font-weight: bold;
}

tr:nth-child(even) {
  background-color: #f9f9f9; /* Light gray for even rows */
}

tr:hover {
  background-color: #f1f1f1; /* Highlight on hover */
}

  </style>
</head>
<body>
  <!-- Container for the timetable -->
  <div class="timetable-container">
    <!-- "Show All Day Timetable" Button -->
    <button class="all-timetable-btn"><a href="/show-all-day-timetable-student">Show All Day</a></button>

    <!-- Timetable Box -->
    <div class="timetable-box">
      {{#if data}}
      <h2>Today's Timetable - {{data.day}}</h2>
      <table>
        <thead>
          <tr>
            <th>Period</th>
            <th>Subject</th>
            <th>Starting Time</th>
            <th>Ending Time</th>
            <th>Tutor</th>
          </tr>
        </thead>
        <tbody>
          <tr>
            <td>1st Period</td>
            <td>{{data.firstPeriod.0.subject}}</td>
            <td>{{data.firstPeriod.0.startingTime}}</td>
            <td>{{data.firstPeriod.0.endingTime}}</td>
            <td>{{data.firstPeriod.0.tutor}}</td>
          </tr>
          <tr>
            <td>2nd Period</td>
            <td>{{data.secondPeriod.0.subject}}</td>
            <td>{{data.secondPeriod.0.startingTime}}</td>
            <td>{{data.secondPeriod.0.endingTime}}</td>
            <td>{{data.secondPeriod.0.tutor}}</td>
          </tr>
          <tr>
            <td>3rd Period</td>
            <td>{{data.thirdPeriod.0.subject}}</td>
            <td>{{data.thirdPeriod.0.startingTime}}</td>
            <td>{{data.thirdPeriod.0.endingTime}}</td>
            <td>{{data.thirdPeriod.0.tutor}}</td>
          </tr>
          <tr>
            <td>4th Period</td>
            <td>{{data.fourthPeriod.0.subject}}</td>
            <td>{{data.fourthPeriod.0.startingTime}}</td>
            <td>{{data.fourthPeriod.0.endingTime}}</td>
            <td>{{data.fourthPeriod.0.tutor}}</td>
          </tr>
          <tr>
            <td>5th Period</td>
            <td>{{data.fifthPeriod.0.subject}}</td>
            <td>{{data.fifthPeriod.0.startingTime}}</td>
            <td>{{data.fifthPeriod.0.endingTime}}</td>
            <td>{{data.fifthPeriod.0.tutor}}</td>
          </tr>
        </tbody>
      </table>
      {{else}}
      <h3 style="color:#FFB300;">Today's Timetabel Is Not Available</h3>
      {{/if}}
    </div>
  </div>
</body>
</html>
